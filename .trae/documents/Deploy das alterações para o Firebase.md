Já apliquei as alterações no código. Agora, para que elas tenham efeito, precisamos realizar o deploy das funções para o Firebase.

**Plano de Ação:**
1.  Executar o comando de deploy do Firebase para atualizar as Cloud Functions com a nova configuração de assinatura (aceite de termo).

Comando a ser executado: `firebase deploy --only functions` no diretório `firebase`.